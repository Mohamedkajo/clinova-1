import { escapeAttribute, escapeHtml } from "./safe-html.js";

export const emptyPatientFilters = Object.freeze({ query: "", status: "", therapistId: "", upcoming: "all", recent: "all", page: 1, pageSize: 20 });

const copy = {
  en: {
    search: "Search by name, phone, or email", allStatuses: "All statuses", allTherapists: "All therapists", upcoming: "Upcoming appointment", allUpcoming: "Any appointment", hasUpcoming: "Has upcoming", noUpcoming: "No upcoming", recent: "Recent activity", anyTime: "Any time", days30: "Last 30 days", days90: "Last 90 days", apply: "Search", clear: "Clear filters", patient: "Patient", contact: "Contact", status: "Status", therapist: "Assigned therapist", lastVisit: "Last visit", nextVisit: "Next appointment", activity: "Last activity", actions: "Actions", profile: "Open profile", edit: "Edit", remove: "Delete", none: "Not available", loading: "Loading patients…", error: "Patients could not be loaded.", retry: "Try again", empty: "No patients match these filters.", page: "Page", previous: "Previous", next: "Next", results: "patients", close: "Close", profileLoading: "Loading patient profile…", profileError: "Patient profile could not be loaded.", overview: "Patient overview", details: "Administrative details", source: "Source", tags: "Tags", created: "Created", updated: "Updated", notes: "Clinical notes", upcomingCard: "Upcoming appointment", recentCard: "Most recent visit", indicators: "Current indicators", appointments: "Appointments", files: "Files", timeline: "Timeline", noAppointments: "No appointments recorded.", noFiles: "No files recorded.", noTimeline: "No timeline events recorded.", completed: "Completed", consents: "Consents", appointmentCount: "Appointments", fileCount: "Files", service: "Service", date: "Date", time: "Time", appointmentStatus: "Appointment status", payment: "Payment", paid: "Paid", unpaid: "Unpaid", deposit: "Deposit", openAppointment: "Open appointment", addNote: "Add clinical note", notePlaceholder: "Clinical note", uploadFile: "Upload file", fileName: "File name", fileNotes: "File notes", patient_created: "Patient profile created", client_created: "Patient profile created", stage_changed: "Patient status changed", clinical_note: "Clinical note added", appointment: "Appointment", file_uploaded: "File uploaded", consent_signed: "Consent signed", lead: "Lead", contacted: "Contacted", qualified: "Qualified", active: "Active", follow_up: "Follow-up", vip: "VIP", inactive: "Inactive", lost: "Lost", pending: "Pending", done: "Completed", cancelled: "Cancelled",
  },
  he: {
    search: "חיפוש לפי שם, טלפון או אימייל", allStatuses: "כל הסטטוסים", allTherapists: "כל המטפלים", upcoming: "תור קרוב", allUpcoming: "כל מצב", hasUpcoming: "יש תור קרוב", noUpcoming: "אין תור קרוב", recent: "פעילות אחרונה", anyTime: "כל תקופה", days30: "30 הימים האחרונים", days90: "90 הימים האחרונים", apply: "חיפוש", clear: "ניקוי סינון", patient: "מטופל", contact: "פרטי קשר", status: "סטטוס", therapist: "מטפל/ת משויך/ת", lastVisit: "ביקור אחרון", nextVisit: "תור הבא", activity: "פעילות אחרונה", actions: "פעולות", profile: "פתיחת תיק", edit: "עריכה", remove: "מחיקה", none: "לא זמין", loading: "טוען מטופלים…", error: "לא ניתן לטעון את המטופלים.", retry: "ניסיון נוסף", empty: "לא נמצאו מטופלים התואמים לסינון.", page: "עמוד", previous: "הקודם", next: "הבא", results: "מטופלים", close: "סגירה", profileLoading: "טוען תיק מטופל…", profileError: "לא ניתן לטעון את תיק המטופל.", overview: "סקירת מטופל", details: "פרטים מנהליים", source: "מקור", tags: "תגיות", created: "נוצר", updated: "עודכן", notes: "הערות קליניות", upcomingCard: "התור הקרוב", recentCard: "הביקור האחרון", indicators: "מדדים נוכחיים", appointments: "תורים", files: "קבצים", timeline: "ציר זמן", noAppointments: "אין תורים מתועדים.", noFiles: "אין קבצים מתועדים.", noTimeline: "אין אירועים מתועדים בציר הזמן.", completed: "הושלמו", consents: "הסכמות", appointmentCount: "תורים", fileCount: "קבצים", service: "שירות", date: "תאריך", time: "שעה", appointmentStatus: "סטטוס תור", payment: "תשלום", paid: "שולם", unpaid: "לא שולם", deposit: "מקדמה", openAppointment: "פתיחת התור", addNote: "הוספת הערה קלינית", notePlaceholder: "הערה קלינית", uploadFile: "העלאת קובץ", fileName: "שם הקובץ", fileNotes: "הערות לקובץ", patient_created: "נוצר תיק מטופל", client_created: "נוצר תיק מטופל", stage_changed: "סטטוס המטופל השתנה", clinical_note: "נוספה הערה קלינית", appointment: "תור", file_uploaded: "הועלה קובץ", consent_signed: "נחתמה הסכמה", lead: "ליד", contacted: "נוצר קשר", qualified: "מתאים", active: "פעיל", follow_up: "מעקב", vip: "VIP", inactive: "לא פעיל", lost: "לא רלוונטי", pending: "ממתין", done: "הושלם", cancelled: "בוטל",
  },
  ar: {
    search: "البحث بالاسم أو الهاتف أو البريد", allStatuses: "كل الحالات", allTherapists: "كل المعالجين", upcoming: "موعد قادم", allUpcoming: "كل الحالات", hasUpcoming: "يوجد موعد قادم", noUpcoming: "لا يوجد موعد قادم", recent: "نشاط حديث", anyTime: "كل الفترات", days30: "آخر 30 يومًا", days90: "آخر 90 يومًا", apply: "بحث", clear: "مسح المرشحات", patient: "المريض", contact: "الاتصال", status: "الحالة", therapist: "المعالج المسؤول", lastVisit: "الزيارة الأخيرة", nextVisit: "الموعد التالي", activity: "آخر نشاط", actions: "الإجراءات", profile: "فتح الملف", edit: "تعديل", remove: "حذف", none: "غير متوفر", loading: "جارٍ تحميل المرضى…", error: "تعذّر تحميل المرضى.", retry: "المحاولة مجددًا", empty: "لا يوجد مرضى مطابقون للمرشحات.", page: "صفحة", previous: "السابق", next: "التالي", results: "مرضى", close: "إغلاق", profileLoading: "جارٍ تحميل ملف المريض…", profileError: "تعذّر تحميل ملف المريض.", overview: "ملخص المريض", details: "تفاصيل إدارية", source: "المصدر", tags: "الوسوم", created: "تاريخ الإنشاء", updated: "آخر تحديث", notes: "ملاحظات سريرية", upcomingCard: "الموعد القادم", recentCard: "الزيارة الأخيرة", indicators: "المؤشرات الحالية", appointments: "المواعيد", files: "الملفات", timeline: "الخط الزمني", noAppointments: "لا توجد مواعيد مسجلة.", noFiles: "لا توجد ملفات مسجلة.", noTimeline: "لا توجد أحداث مسجلة.", completed: "مكتملة", consents: "موافقات", appointmentCount: "مواعيد", fileCount: "ملفات", service: "الخدمة", date: "التاريخ", time: "الوقت", appointmentStatus: "حالة الموعد", payment: "الدفع", paid: "مدفوع", unpaid: "غير مدفوع", deposit: "عربون", openAppointment: "فتح الموعد", addNote: "إضافة ملاحظة سريرية", notePlaceholder: "ملاحظة سريرية", uploadFile: "رفع ملف", fileName: "اسم الملف", fileNotes: "ملاحظات الملف", patient_created: "تم إنشاء ملف المريض", client_created: "تم إنشاء ملف المريض", stage_changed: "تغيّرت حالة المريض", clinical_note: "أضيفت ملاحظة سريرية", appointment: "موعد", file_uploaded: "تم رفع ملف", consent_signed: "تم توقيع موافقة", lead: "عميل محتمل", contacted: "تم التواصل", qualified: "مؤهل", active: "نشط", follow_up: "متابعة", vip: "VIP", inactive: "غير نشط", lost: "غير مهتم", pending: "قيد الانتظار", done: "مكتمل", cancelled: "ملغي",
  },
};

const t = (language, key) => copy[language]?.[key] || copy.en[key] || key;
const attr = (value) => escapeAttribute(value ?? "");
const safe = (value) => escapeHtml(value ?? "");

function formatDate(value, language, includeTime = false) {
  if (!value) return t(language, "none");
  const date = new Date(String(value).includes("T") ? value : `${String(value).replace(" ", "T")}Z`);
  if (Number.isNaN(date.valueOf())) return safe(value);
  return new Intl.DateTimeFormat(language === "he" ? "he-IL" : language === "ar" ? "ar" : "en", includeTime ? { dateStyle: "medium", timeStyle: "short" } : { dateStyle: "medium" }).format(date);
}

function statusPill(value, language) {
  return `<span class="patient-status patient-status-${attr(value || "lead")}">${safe(t(language, value || "lead"))}</span>`;
}

function boundary(kind, language, message = "") {
  if (kind === "loading") return `<div class="patient-boundary" aria-busy="true"><div class="boundary-spinner" aria-hidden="true"></div><p>${safe(message || t(language, "loading"))}</p></div>`;
  if (kind === "error") return `<div class="patient-boundary" role="alert"><p>${safe(message || t(language, "error"))}</p><button class="btn secondary" type="button" data-patient-retry>${safe(t(language, "retry"))}</button></div>`;
  return `<div class="patient-boundary"><p>${safe(message)}</p></div>`;
}

function filterOption(value, current, label) {
  return `<option value="${attr(value)}" ${String(value) === String(current) ? "selected" : ""}>${safe(label)}</option>`;
}

function patientActions(patient, language, canManage) {
  return `<div class="patient-actions"><button class="btn secondary" type="button" data-profile="${attr(patient.id)}">${safe(t(language, "profile"))}</button>${canManage ? `<button class="btn ghost" type="button" data-edit="clients" data-id="${attr(patient.id)}">${safe(t(language, "edit"))}</button><button class="btn danger" type="button" data-delete="clients" data-id="${attr(patient.id)}">${safe(t(language, "remove"))}</button>` : ""}</div>`;
}

export function renderPatientWorkspace({ language = "en", workspace, canManage = false }) {
  const filters = workspace.filters || emptyPatientFilters;
  const options = workspace.data?.filterOptions || { statuses: [], therapists: [] };
  const toolbar = `<form class="patient-toolbar" data-patient-search>
    <label class="patient-search"><span class="sr-only">${safe(t(language, "search"))}</span><input name="query" value="${attr(filters.query)}" placeholder="${attr(t(language, "search"))}"></label>
    <label><span>${safe(t(language, "status"))}</span><select name="status">${filterOption("", filters.status, t(language, "allStatuses"))}${options.statuses.map((value) => filterOption(value, filters.status, t(language, value))).join("")}</select></label>
    <label><span>${safe(t(language, "therapist"))}</span><select name="therapistId">${filterOption("", filters.therapistId, t(language, "allTherapists"))}${options.therapists.map((item) => filterOption(item.id, filters.therapistId, item.name)).join("")}</select></label>
    <label><span>${safe(t(language, "upcoming"))}</span><select name="upcoming">${filterOption("all", filters.upcoming, t(language, "allUpcoming"))}${filterOption("yes", filters.upcoming, t(language, "hasUpcoming"))}${filterOption("no", filters.upcoming, t(language, "noUpcoming"))}</select></label>
    <label><span>${safe(t(language, "recent"))}</span><select name="recent">${filterOption("all", filters.recent, t(language, "anyTime"))}${filterOption("30", filters.recent, t(language, "days30"))}${filterOption("90", filters.recent, t(language, "days90"))}</select></label>
    <button class="btn" type="submit">${safe(t(language, "apply"))}</button><button class="btn ghost" type="button" data-patient-clear>${safe(t(language, "clear"))}</button>
  </form>`;
  if (workspace.status === "loading" || workspace.status === "idle") return `<section class="patient-workspace">${toolbar}${boundary("loading", language)}</section>`;
  if (workspace.status === "error") return `<section class="patient-workspace">${toolbar}${boundary("error", language, workspace.error)}</section>`;
  const data = workspace.data || { items: [], page: 1, total: 0, totalPages: 0 };
  if (!data.items?.length) return `<section class="patient-workspace">${toolbar}${boundary("empty", language, t(language, "empty"))}</section>`;

  const rows = data.items.map((patient) => `<tr><td><button class="patient-name-link" type="button" data-profile="${attr(patient.id)}"><strong>${safe(patient.name)}</strong><small>#${safe(patient.id)}</small></button></td><td><a href="tel:${attr(patient.phone)}">${safe(patient.phone || t(language, "none"))}</a><small>${safe(patient.email || "")}</small></td><td>${statusPill(patient.stage, language)}</td><td>${safe(patient.therapist?.name || t(language, "none"))}</td><td>${safe(formatDate(patient.lastAppointmentDate, language))}</td><td>${safe(formatDate(patient.nextAppointmentDate, language))}</td><td>${patientActions(patient, language, canManage)}</td></tr>`).join("");
  const cards = data.items.map((patient) => `<article class="patient-card"><header><button class="patient-name-link" type="button" data-profile="${attr(patient.id)}"><strong>${safe(patient.name)}</strong><small>#${safe(patient.id)}</small></button>${statusPill(patient.stage, language)}</header><dl><div><dt>${safe(t(language, "contact"))}</dt><dd>${safe(patient.phone || t(language, "none"))}<small>${safe(patient.email || "")}</small></dd></div><div><dt>${safe(t(language, "therapist"))}</dt><dd>${safe(patient.therapist?.name || t(language, "none"))}</dd></div><div><dt>${safe(t(language, "lastVisit"))}</dt><dd>${safe(formatDate(patient.lastAppointmentDate, language))}</dd></div><div><dt>${safe(t(language, "nextVisit"))}</dt><dd>${safe(formatDate(patient.nextAppointmentDate, language))}</dd></div></dl>${patientActions(patient, language, canManage)}</article>`).join("");
  return `<section class="patient-workspace">${toolbar}<div class="patient-results-summary"><strong>${safe(data.total)}</strong> ${safe(t(language, "results"))}</div><div class="patient-table-wrap"><table class="patient-table"><thead><tr><th>${safe(t(language, "patient"))}</th><th>${safe(t(language, "contact"))}</th><th>${safe(t(language, "status"))}</th><th>${safe(t(language, "therapist"))}</th><th>${safe(t(language, "lastVisit"))}</th><th>${safe(t(language, "nextVisit"))}</th><th>${safe(t(language, "actions"))}</th></tr></thead><tbody>${rows}</tbody></table></div><div class="patient-mobile-list">${cards}</div><nav class="patient-pagination" aria-label="${attr(t(language, "page"))}"><button class="btn secondary" type="button" data-patient-page="${attr(data.page - 1)}" ${data.page <= 1 ? "disabled" : ""}>${safe(t(language, "previous"))}</button><span>${safe(t(language, "page"))} ${safe(data.page)} / ${safe(Math.max(1, data.totalPages))}</span><button class="btn secondary" type="button" data-patient-page="${attr(data.page + 1)}" ${data.page >= data.totalPages ? "disabled" : ""}>${safe(t(language, "next"))}</button></nav></section>`;
}

function appointmentSummary(appointment, language) {
  if (!appointment) return `<p class="muted">${safe(t(language, "none"))}</p>`;
  return `<div class="patient-appointment-summary"><strong>${safe(appointment.serviceName || t(language, "appointment"))}</strong><span>${safe(formatDate(appointment.date, language))} · ${safe(appointment.time || "")}</span><span>${statusPill(appointment.status, language)}</span><button class="btn ghost" type="button" data-patient-appointment-details="${attr(appointment.id)}">${safe(t(language, "openAppointment"))}</button></div>`;
}

function timelineItem(event, language) {
  const financial = event.financial ? `<span>${safe(t(language, "payment"))}: ${safe(t(language, event.financial.paymentStatus))} · ${safe(event.financial.paidAmount)}</span>` : "";
  const linked = event.related?.appointmentId ? `<button class="btn ghost" type="button" data-patient-appointment-details="${attr(event.related.appointmentId)}">${safe(t(language, "openAppointment"))}</button>` : "";
  return `<li class="patient-timeline-item"><span class="patient-timeline-dot" aria-hidden="true"></span><div><header><strong>${safe(t(language, event.type))}</strong><time>${safe(formatDate(event.occurredAt, language, true))}</time></header>${event.description ? `<p>${safe(event.description)}</p>` : ""}${event.actor ? `<span>${safe(event.actor)}</span>` : ""}${financial}${linked}</div></li>`;
}

export function renderPatientProfile({ language = "en", status = "ready", data = null, error = "" }) {
  const close = `<button class="icon-button" type="button" data-close-patient-profile aria-label="${attr(t(language, "close"))}">×</button>`;
  if (status === "loading") return `<div class="patient-profile-layer"><button class="patient-profile-scrim" type="button" data-close-patient-profile aria-label="${attr(t(language, "close"))}"></button><section class="patient-profile-panel"><header><h2>${safe(t(language, "overview"))}</h2>${close}</header>${boundary("loading", language, t(language, "profileLoading"))}</section></div>`;
  if (status === "error") return `<div class="patient-profile-layer"><button class="patient-profile-scrim" type="button" data-close-patient-profile aria-label="${attr(t(language, "close"))}"></button><section class="patient-profile-panel"><header><h2>${safe(t(language, "overview"))}</h2>${close}</header>${boundary("error", language, error || t(language, "profileError"))}</section></div>`;
  const patient = data.patient || {};
  const capabilities = data.capabilities || {};
  const indicators = data.indicators || {};
  const appointments = (data.appointments || []).slice(0, 8);
  const files = data.files || [];
  return `<div class="patient-profile-layer"><button class="patient-profile-scrim" type="button" data-close-patient-profile aria-label="${attr(t(language, "close"))}"></button><section class="patient-profile-panel" aria-labelledby="patientProfileTitle"><header><div><span>${safe(t(language, "overview"))}</span><h2 id="patientProfileTitle">${safe(patient.name || "")}</h2></div>${close}</header><div class="patient-profile-body">
    <section class="patient-identity"><div class="patient-avatar" aria-hidden="true">${safe((patient.firstName?.[0] || "") + (patient.lastName?.[0] || ""))}</div><div><h3>${safe(patient.name || "")}</h3><p>${safe(patient.phone || t(language, "none"))} · ${safe(patient.email || t(language, "none"))}</p><div>${statusPill(patient.stage, language)}<span>${safe(patient.therapist?.name || t(language, "none"))}</span></div></div></section>
    <div class="patient-profile-grid"><section class="patient-profile-card"><h3>${safe(t(language, "details"))}</h3><dl><div><dt>${safe(t(language, "source"))}</dt><dd>${safe(patient.source || t(language, "none"))}</dd></div><div><dt>${safe(t(language, "tags"))}</dt><dd>${safe(patient.tags?.join(", ") || t(language, "none"))}</dd></div><div><dt>${safe(t(language, "created"))}</dt><dd>${safe(formatDate(patient.createdAt, language, true))}</dd></div><div><dt>${safe(t(language, "updated"))}</dt><dd>${safe(formatDate(patient.updatedAt, language, true))}</dd></div></dl>${capabilities.clinicalNotes ? `<div class="patient-clinical-notes"><strong>${safe(t(language, "notes"))}</strong><p>${safe(patient.notes || t(language, "none"))}</p></div>` : ""}</section>
    <section class="patient-profile-card"><h3>${safe(t(language, "upcomingCard"))}</h3>${appointmentSummary(data.upcomingAppointment, language)}</section><section class="patient-profile-card"><h3>${safe(t(language, "recentCard"))}</h3>${appointmentSummary(data.recentAppointment, language)}</section>
    <section class="patient-profile-card patient-indicators"><h3>${safe(t(language, "indicators"))}</h3><div><span><strong>${safe(indicators.appointmentCount || 0)}</strong>${safe(t(language, "appointmentCount"))}</span><span><strong>${safe(indicators.completedCount || 0)}</strong>${safe(t(language, "completed"))}</span><span><strong>${safe(indicators.fileCount || 0)}</strong>${safe(t(language, "fileCount"))}</span><span><strong>${safe(indicators.consentCount || 0)}</strong>${safe(t(language, "consents"))}</span></div></section></div>
    <section class="patient-profile-section"><h3>${safe(t(language, "appointments"))}</h3><div class="patient-appointment-list">${appointments.map((item) => `<article><div><strong>${safe(item.serviceName)}</strong><span>${safe(formatDate(item.date, language))} · ${safe(item.time)}</span></div>${statusPill(item.status, language)}<button class="btn ghost" type="button" data-patient-appointment-details="${attr(item.id)}">${safe(t(language, "openAppointment"))}</button></article>`).join("") || `<p class="muted">${safe(t(language, "noAppointments"))}</p>`}</div></section>
    <section class="patient-profile-section"><h3>${safe(t(language, "files"))}</h3><div class="patient-file-list">${files.map((file) => `<article><div><a href="${attr(file.url)}" target="_blank" rel="noopener">${safe(file.name)}</a><span>${safe(file.notes || file.originalName || "")}</span></div>${capabilities.write ? `<button class="btn danger" type="button" data-delete-file="${attr(file.id)}" data-client="${attr(patient.id)}">${safe(t(language, "remove"))}</button>` : ""}</article>`).join("") || `<p class="muted">${safe(t(language, "noFiles"))}</p>`}</div>${capabilities.write ? `<form id="clientFileForm" class="patient-inline-form"><input name="name" placeholder="${attr(t(language, "fileName"))}"><input name="file" type="file" accept="image/jpeg,image/png,image/webp,application/pdf" required><input name="notes" placeholder="${attr(t(language, "fileNotes"))}"><button class="btn" type="submit">${safe(t(language, "uploadFile"))}</button></form>` : ""}</section>
    <section class="patient-profile-section"><h3>${safe(t(language, "timeline"))}</h3>${capabilities.write && capabilities.clinicalNotes ? `<form id="clientNoteForm" class="patient-inline-form"><input name="note" placeholder="${attr(t(language, "notePlaceholder"))}" required><button class="btn secondary" type="submit">${safe(t(language, "addNote"))}</button></form>` : ""}<ol class="patient-timeline">${(data.timeline || []).map((event) => timelineItem(event, language)).join("") || `<li class="muted">${safe(t(language, "noTimeline"))}</li>`}</ol></section>
  </div></section></div>`;
}
